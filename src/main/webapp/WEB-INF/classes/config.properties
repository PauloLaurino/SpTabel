# config.properties
# Configurações do Banco de Dados
db.url=jdbc:mysql://100.75.153.127:3306/sptabel
db.username=root
db.password=k15720
db.pool.max.size=10
db.pool.min.idle=5
db.connection.timeout=30000
db.validation.timeout=5000

# Configurações da Aplicação
app.name=Selador
app.version=1.0.0
app.maker.integration.enabled=true

# Configurações de Selo
selo.tipo.padrao=0004
selo.codigo.tipo.ato=701
selo.validade.meses=12

# Configurações da Interface
interface.pagina.tamanho=50
interface.timeout.busca=30000
interface.max.dias.consulta=30

# Configurações de Log
log.nivel.minimo=INFO
log.diretorio=logs
log.max.size.mb=10
log.retention.days=30

# Configurações de Segurança
security.token.expiration.minutes=30
security.max.login.attempts=5
security.password.min.length=8

# Configurações de Email (opcional)
email.smtp.host=
email.smtp.port=587
email.smtp.username=
email.smtp.password=
email.from=noreply@selador.com.br