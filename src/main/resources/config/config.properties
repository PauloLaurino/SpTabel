# Configurações do Banco de Dados
db.url=jdbc:mariadb://localhost:3306/sptabel
db.username=root
db.password=k15720
db.pool.max.size=10
db.pool.min.idle=2
db.connection.timeout=30000
db.idle.timeout=600000
db.max.lifetime=1800000

# Configurações da Aplicação
app.name=Selador
app.version=1.0.0
app.maker.integration.enabled=true

# Configurações de Selo
selo.tipo.padrao=0004
selo.codigo.tipo.ato=701

# Configurações da API
api.cors.enabled=true
api.auth.enabled=false
api.default.timeout=30000

# Configurações de Log
log.level=INFO
log.directory=logs
log.max.size.mb=10